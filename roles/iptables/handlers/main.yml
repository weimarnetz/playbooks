---
# Handlers für iptables

- name: apply iptables rules
  command: iptables-restore < /etc/iptables/rules.v4
  when: ansible_os_family == "Debian"

- name: apply iptables rules (RedHat)
  command: iptables-restore < /etc/iptables/rules.v4
  when: ansible_os_family == "RedHat"

